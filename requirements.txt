# Core — Claude Agent SDK
anthropic>=0.69.0

# Financial document parsing
pandas>=2.0.0
openpyxl>=3.1.0          # .xlsx
xlrd>=2.0.1              # legacy .xls

# Geospatial / parcel & land-area analysis
shapely>=2.0.0          # geometry, areas, centroids, bounds

# Config
python-dotenv>=1.0.0

# Optional (uncomment if you work with shapefiles / projected GIS data):
# geopandas>=0.14.0
# pyproj>=3.6.0

# Optional — live Go/No-Go functions inside Excel (=GONOGO_ASK, =GONOGO, ...):
# xlwings>=0.30.0
