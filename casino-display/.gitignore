# Installed packages (vendor/ holds the copies the app actually loads)
node_modules/
package-lock.json
package.json

# Render artifacts / logs
render.log
render2.log
frames/
*.tmp

# Rendered videos are delivered as files, not stored in git (keep repo light)
*.mp4
*.webm
render3.log
render4.log

# any render/probe logs
*.log
render5.log

# Nug video build artifacts (regenerable)
video/audio/
*.wav
video/*.png
video/assets/
video/clips/

# but keep the real user-provided source images
!casino-display/video/source_images/
